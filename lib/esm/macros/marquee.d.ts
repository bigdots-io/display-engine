import { MacroMarqueeConfig, PixelChangeCallback } from "../types.js";
export declare const startMarquee: (config: MacroMarqueeConfig, macroIndex: number, onPixelChange: PixelChangeCallback) => void;
